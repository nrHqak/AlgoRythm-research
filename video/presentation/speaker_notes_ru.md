# Заметки для видеозащиты AlgoRythm

Основной маршрут рассчитан на **1:55 (115 секунд)**. Переключение ручное; автоматический режим нужен только как ориентир репетиции. Текст речи хранится отдельно в `script_ru.txt` и не выводится на экран.

## Сцена 1 — 0:00–0:16

- **Сигнал переключения на сцену:** начало записи, без вступительной заставки.
- **Что видно:** AlgoRythm; `[ИМЯ ФАМИЛИЯ] · Атырау`; формулировка проблемы; небольшой, явно подписанный учебный пример логической ошибки в поддерживаемом продуктом бинарном поиске.
- **Сигнал следующей сцены:** «Так появился AlgoRythm…»
- **Не произносить:** фрагмент кода не является экспериментальным результатом и не связан с программами выборки. Строка `left = mid + 1` намеренно показана вместо фиксации найденного индекса, поэтому демонстрационная программа может завершиться с неверным ответом.

## Сцена 2 — 0:16–0:29

- **Сигнал переключения:** «Так появился AlgoRythm…»
- **Что видно:** функциональная схема текущего экрана — код, состояние массива, шаг trace, элементы управления и назначение ментора. Текст поля ментора взят из текущей русской локализации продукта; никакой ответ модели не имитируется.
- **Сигнал следующей сцены:** «При разработке возник научный вопрос…»
- **Научное/техническое пояснение:** схема собрана по фактическим компонентам `PlaygroundPage`, `VisualCanvas`, `StepPlayer` и `MentorChat` продуктового репозитория; пример бинарного поиска взят из `EXAMPLES`. Это не снимок работающей песочницы. Публичный деплой 2026-09-14 перенаправлял `/playground` на страницу входа, а кнопка demo account не отображалась, поэтому защищённый экран без авторизации не снимался.
- **Граница утверждения:** «Вопросы и подсказки вместо готового решения» — назначение интерфейса, а не гарантия безошибочного поведения AI и не доказательство учебного эффекта.

## Сцена 3 — 0:29–0:49

- **Сигнал переключения:** «При разработке возник научный вопрос…»
- **Что видно:** один исходный код разделяется на два условия; основное сравнение выделено как **B ↔ C**.
- **Сигнал следующей сцены:** «Для пилота я отобрал…»
- **B:** общая отладочная подсказка. Короткий фрагмент переведён из замороженного текста: проверка границ циклов и направления сравнений.
- **C:** подсказка по структуре алгоритма. Короткий фрагмент переведён из замороженного prior для `binary_search`: проверка вычисления средней точки.
- **Общее для B и C:** одна модель; один и тот же исходный код; один системный prompt и общий шаблон; одинаковое место блока; сопоставимые формат, тон, число пунктов и длина в заранее принятом допуске.
- **Различие:** содержание блока дополнительной подсказки. Не говорить «абсолютно одинаковая длина»: максимальное отклонение было 8 слов.
- **Остаточное ограничение:** семантическая релевантность программы контролировалась не по отдельной непаттерновой оси; это известная граница пилота.

## Сцена 4 — 0:49–1:08

- **Сигнал переключения:** «Для пилота я отобрал…»
- **Что видно:** короткий путь подготовки выборки; центральная формула **30 × 4 × 5 = 600 вызовов**; четыре класса; две сессии и главное B ↔ C.
- **Сигнал следующей сцены:** «По основной метрике…»
- **Единица анализа:** программа. 600 вызовов — не 600 независимых примеров; эффективное число парных наблюдений в главном сравнении равно 30.
- **Классы:** динамическое программирование — 8; полный перебор / прямая реализация — 8; бинарный поиск — 7; обход графа — 7.
- **Условия:** `A_G` и B — сессия G; `A_P` и C — сессия P. A — без дополнительного блока подсказки, но не «без какого-либо руководства». Нулевые условия нужны для оценки межсессионного дрейфа.
- **Модель:** одна замороженная `z-ai/glm-5.3-flash`, temperature 0, reasoning effort `low`; Claude и Codex были аннотаторами паттернов, а не конкурирующими локализаторами.
- **Полная воронка, если спросят:** 232 независимо размеченные программы → 182 точных совпадения, включая 25 `outside_vocabulary` → 157 совпадений внутри словаря → 109 программ четырёх выбранных классов → 30 в финальной выборке. Совпадение двух модельных меток — фильтр надёжности, не доказательство истинности.
- **Предрегистрация:** протокол, prompts, priors, модель, поставщик, параметры, число повторений и план статистического анализа были заморожены до финального сбора результатов.

## Сцена 5 — 1:08–1:32

### Состояние 5A — ориентировочно 1:08–1:20

- **Сигнал переключения:** «По основной метрике…»
- **Что видно:** только основная группа столбцов Top-1; B и C буквально одинаковой высоты.
- **Числа:** B = 12/30 (40,0%); C = 12/30 (40,0%); Δ = 0 п. п.; точный критерий Макнемара `p = 1,0`; 95% бутстреп-ДИ разницы C − B: [−16,67; +16,67] п. п.
- **Интерпретация:** преимуществa по Top-1 не обнаружено. Равные оценки не доказывают эквивалентность методов и не доказывают отсутствие эффекта.

### Состояние 5B — ориентировочно 1:20–1:32

- **Сигнал внутреннего шага:** «В первых трёх и пяти позициях…» — нажать Space/→ ещё раз, оставаясь на сцене 5.
- **Что появляется:** Top-3 и Top-5 на той же шкале 0–100%; основная группа Top-1 остаётся на экране.
- **Top-3:** B = 15/30 (50,0%); C = 18/30 (60,0%); +10 п. п.; Макнемар `p = 0,453125`; 95% ДИ [−6,67; +26,67] п. п.
- **Top-5:** B = 16/30 (53,33%); C = 19/30 (63,33%); +10 п. п.; Макнемар `p = 0,375`; 95% ДИ [−3,33; +23,33] п. п.
- **Обязательная оговорка:** различия вторичные и описательные; статистически убедительного преимущества нет. «+10 п. п.» — абсолютная разница долей, не относительный прирост 10%.
- **Правило агрегации:** для каждой программы Top-K считался успехом, если соответствующий исход возник минимум в 3 из 5 повторений. Пять рейтингов не объединялись в один.
- **EXAM\*, если спросят:** B = 0,510798; C = 0,445055; ниже лучше; разница −0,065743; Wilcoxon `p = 0,124`; 95% ДИ [−0,1622; +0,0283]. Метрика имеет цензурированный знаменатель физических строк и не сравнима напрямую с опубликованным EXAM.
- **По классам, только если спросят:** описательно C − B по Top-1: бинарный поиск 0 п. п.; полный перебор +25 п. п.; динамическое программирование −25 п. п.; обход графа 0 п. п. Подвыборки слишком малы для подтверждающих утверждений.

## Сцена 6 — 1:32–1:55

- **Сигнал переключения:** «Это проверка отдельного механизма…»
- **Что видно:** слева — что действительно проверено; справа — будущий этап, явно помеченный как ещё не проверенный.
- **Финальная фраза на экране:** «Понять собственное решение».
- **Граница вывода:** пилот проверял конкретную текстовую стратегию локализации на 30 программах и одной модели; он не проверял учебный эффект визуализации или помощь ученикам.
- **Следующее исследование:** подсказка, обусловленная шагами исполнения; затем отдельное исследование с участием учащихся и требуемым этическим одобрением до сбора данных.
- **Не утверждать:** что визуализация доказанно улучшает обучение, что платформа «не работает», что подсказка помогает отдельным классам, или что эффективность доказана.

## Режим размещения

- **MONITOR:** содержание занимает экран за автором; используйте минимум мелких деталей.
- **OVERLAY:** визуал занимает 59% кадра, оставляя спокойные 41% для автора. Кнопка «Визуал слева/справа» меняет сторону; выбирайте сторону в зависимости от положения автора в кадре.
